function addNumbers(a, b) {
    return a + b;
  }

  function subNumbers(a, b) {
    return a - b;
  }
  
  // Function to greet a user
  function greetUser(name) {
    return "Hello, " + name + "!";
  }
  
  // Function to calculate the square of a number
  function square(number) {
    return number * number;
  }
  
  // Example usage of the functions
  const sum = addNumbers(5, 3); //5+3 = 8
  const greeting = greetUser("John");
  const squaredValue = square(4);


  
  
  console.log("Sum:", sum);  // 
  console.log(greeting);
  console.log("Squared Value:", squaredValue);
  /*In this code:
  
  addNumbers is a function that takes two parameters (a and b) and returns their sum.
  greetUser is a function that takes a name parameter and returns a greeting message.
  square is a function that takes a number parameter and returns the square of that number.
  The example usage of these functions demonstrates how to call them with different arguments and print the results to the console.*/
  
  
  
  
  
  